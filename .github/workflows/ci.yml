name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Public repository hygiene
        run: python scripts/check_public_hygiene.py
      - name: Documentation authority
        env:
          GITHUB_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          GITHUB_EVENT_BEFORE: ${{ github.event.before }}
        run: python scripts/check_documentation_authority.py
      - name: Install locked environment
        run: |
          python -m pip install -r requirements.lock.txt
          python -m pip install -e . --no-deps
      - name: experiment-schema
        run: python scripts/check_research_methodology.py --check experiment-schema
      - name: experiment-freeze-integrity
        run: python scripts/check_research_methodology.py --check experiment-freeze-integrity
      - name: experiment-verification
        run: python scripts/check_research_methodology.py --check experiment-verification
      - name: programme-inference-integrity
        run: python scripts/check_research_methodology.py --check programme-inference-integrity
      - name: research-memory
        run: python scripts/check_research_memory.py
      - name: Test
        run: python -m pytest -q
      - name: Lint
        run: python -m ruff check .
      - name: Whitespace
        env:
          EVENT_NAME: ${{ github.event_name }}
          BEFORE_SHA: ${{ github.event.before }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          if [ "$EVENT_NAME" = "pull_request" ]; then
            git diff --check "$PR_BASE_SHA" "$PR_HEAD_SHA"
          elif [ -n "$BEFORE_SHA" ] && [ "$BEFORE_SHA" != "0000000000000000000000000000000000000000" ] && git cat-file -e "$BEFORE_SHA^{commit}"; then
            git diff --check "$BEFORE_SHA" "$GITHUB_SHA"
          else
            git diff --check "$GITHUB_SHA^" "$GITHUB_SHA"
          fi
